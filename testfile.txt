---
- name: test playbook
  hosts: "scalefactory-vbox"
  become: true
  become_user: root
  become_method: sudo
  vars:
    - ansible_ssh_private_key_file: .vagrant/machines/default/virtualbox/private_key

  # roles:
  tasks:
    - name: copy a file
      copy:
        -
